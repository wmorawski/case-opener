import { ItemColorDirective } from './item-color.directive';

describe('ItemColorDirective', () => {
  it('should create an instance', () => {
    const directive = new ItemColorDirective();
    expect(directive).toBeTruthy();
  });
});
